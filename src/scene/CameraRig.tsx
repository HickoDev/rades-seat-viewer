import { OverviewCamera } from '../camera/OverviewCamera';
import { SeatViewController } from '../camera/SeatViewController';

export function CameraRig() {
  return (
    <>
      <OverviewCamera />
      <SeatViewController />
    </>
  );
}
